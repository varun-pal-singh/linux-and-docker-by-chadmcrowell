find / -name "syslog"		    # finding where is syslog files.

/var/log/			            # path where syslog files are find.

cat /var/log/syslog | less	    # show system logs file inside separate window.

cat syslog | grap ubuntu        # searching somethin inside a file